# cfDNA end-motif Fusion pipeline — Python dependencies
# Install with:  pip install -r requirements.txt
torch>=2.0
transformers>=4.40
scikit-learn>=1.3
numpy>=1.24
tqdm>=4.65
pandas
matplotlib

# Note: the DNABERT-4mer weights ("Taykhoom/DNABERT-4mer") are downloaded
# automatically from the Hugging Face Hub on first run (internet required once).
